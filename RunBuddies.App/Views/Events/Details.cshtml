@* @model RunBuddies.ViewModels.EventViewModel
@{
    ViewData["Title"] = "Event Details";
}
<h1>@Model.EventName</h1>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Event Type</dt>
        <dd class="col-sm-10">@Model.EventType</dd>
        <dt class="col-sm-2">Date</dt>
        <dd class="col-sm-10">@Model.DateTime.ToString("MMMM dd, yyyy HH:mm")</dd>
        <dt class="col-sm-2">Location</dt>
        <dd class="col-sm-10">@Model.Location</dd>
        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.Description</dd>
        <dt class="col-sm-2">Organizer</dt>
        <dd class="col-sm-10">@Model.OrganizerName</dd>
    </dl>
</div>
<form asp-action="JoinEvent" asp-route-id="@Model.EventID" method="post">
    <button type="submit" class="btn btn-primary">Join Event</button>
</form>
<div>
    <a asp-action="Index">Back to List</a>
</div> *@